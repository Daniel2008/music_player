import 'package:flutter/material.dart';

/// 均衡器功能已移除，此处保留占位组件以免引用处出错。
class EqualizerPanel extends StatelessWidget {
  const EqualizerPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
